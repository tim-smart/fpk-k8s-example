apiVersion: apps/v1
kind: Deployment
metadata:
  name: mywebapp
  namespace: mywebapp
spec:
  replicas: 10
  selector:
    matchLabels:
      app: mywebapp
  template:
    metadata:
      labels:
        app: mywebapp
    spec:
      containers:
        - name: mywebapp
          image: 'myorg/myappimage:latest'
          ports:
            - containerPort: 8080
          env:
            - name: FOO
              value: bar
          readinessProbe:
            httpGet:
              port: 8080
              path: /
          livenessProbe:
            httpGet:
              port: 8080
              path: /
        - name: super-sidecar
          image: 'myorg/sidecar:v1.0.0'
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 2
